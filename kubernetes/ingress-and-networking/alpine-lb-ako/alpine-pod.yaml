apiVersion: v1
kind: Pod
metadata:
  name: alpine-pod
  labels:
    app: alpine-app
spec:
  containers:
  - name: alpine
    image: alpine:latest
    command: ["sh", "-c", "apk add --no-cache python3 && python3 -m http.server 8080"]
    ports:
    - containerPort: 8080
      name: http
