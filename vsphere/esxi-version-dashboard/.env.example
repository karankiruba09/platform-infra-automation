# Required
VC_USER=administrator@vsphere.local
VC_PASSWORD=changeme

# Optional
VC_INSECURE=true
VC_PARALLEL=20
VC_TIMEOUT_SECONDS=12
VC_UNSET_PROXY=true

# Optional alternate vCenter list path (defaults to ./config/vcenters.txt)
# VCENTERS_FILE=./config/vcenters.txt
